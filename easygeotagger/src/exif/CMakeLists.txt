
SET(EGT_EXIF_SRCS
    egtexifio.cpp
   )

QT4_AUTOMOC(${EGT_EXIF_SRCS})

ADD_LIBRARY (egt_exif SHARED ${EGT_EXIF_SRCS})

TARGET_LINK_LIBRARIES(egt_exif ${QT_LIBRARIES} ${EXIV2_LIBRARY})

INSTALL(TARGETS egt_exif DESTINATION lib)